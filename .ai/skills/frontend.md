# Front-end: React & TypeScript
- Tipagem TypeScript estrita (NUNCA use `any`). Tipos vão na pasta `types/`.
- Lógica de API (Axios) é isolada na pasta `api/` (em `commands` e `queries`).
- Componentize páginas maiores que 100 linhas em `components/`.
- Use classes utilitárias do Tailwind CSS.